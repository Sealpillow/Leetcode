#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>

int main()
{
    int cities,pilots,num,*arr,a,b,count,found,min;
    scanf("%d",&num);
    for(int i =0;i<num;i++)
    {
        scanf("%d %d",&cities,&pilots);
        printf("%d\n",cities-1);
        for(int j=0;j<pilots;j++)
        {
            scanf("%d %d",&a,&b);
        }
    }
}
